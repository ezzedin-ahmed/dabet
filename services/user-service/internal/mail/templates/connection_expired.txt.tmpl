Hi {{.Name}},

Your {{.Platform}} connection ({{.DisplayName}}) has stopped working: the
platform rejected Dabet's access when we last tried to refresh it, which
usually means the access was revoked or the password changed.

Moderation for that channel is not running until you reconnect it:

{{.ConnectionsURL}}

-- Dabet
